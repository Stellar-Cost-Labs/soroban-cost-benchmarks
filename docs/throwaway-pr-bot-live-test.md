# Throwaway: live `pr-comment` bot test

This file exists only so the `pr-comment` bot has a real PR conversation to post
into. It is deleted at the end of the test, along with its branch.

Round 2 — second push, to test update-in-place.
